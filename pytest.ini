[pytest]
markers =
    integration: integration and end-to-end tests
    shell: tests that spawn shell subprocesses (slow — hit real registry)
    unit: pure logic tests with no subprocess or real git repos
